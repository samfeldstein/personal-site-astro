<pre>
  <slot />
</pre>

<style lang="scss">
  pre {
    border-style: solid;
    border-width: 1px;
    border-color: var(--accent);
    border-radius: 4px;
    padding: 1rem;

    code {
      font-family: monospace;
      font-weight: 400;
      font-size: var(--font-small);
    }
  }
</style>
